import React, { useContext } from "react";
import { GlobalContext } from "../Globals";
export default function MoveBall(direction) {
  // const { gameState, setGameState } = useContext(GlobalContext);

  // let ballX = gameState['ballX']
  // if (ballX > 0)
  // setGameState((others) => ({...others, 'ballX': others.ballX - 0.1,}))

  console.log('gameState.ballX');
  // return gameState
}
